Add your photo here as:

    babatunde.jpg

That's it — the About page (about.html) already looks for this exact filename and
will automatically show your photo instead of the "BA" placeholder once it's here.

Tips:
- A square photo (e.g. 800x800px or larger) crops most cleanly into the circular/rounded frame.
- Keep the file size reasonable (under ~500KB) so the page loads quickly.
- If you'd rather use a different filename or format (e.g. babatunde.png), open
  about.html, find the line containing `images/profile/babatunde.jpg` and change
  it to match your filename.
